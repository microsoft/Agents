[project]
name = "agents-local-tests"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "testing-common",
    "pytest",
    "pytest-asyncio",
]

[tool.uv.sources]
testing-common = { path = "testing-common", editable = true }
